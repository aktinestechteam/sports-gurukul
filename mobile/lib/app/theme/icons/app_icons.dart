/// Iconography tokens for the Sports Gurukul design system.
///
/// Iconography uses Material Symbols Rounded (per the approved design
/// system, docs/mobile/01-Design-System.md). Bundled brand icon assets are
/// registered here when delivered; until then, Material Icons from
/// `Icons.*` are used for placeholders.
abstract final class AppIcons {
  const AppIcons();
}
